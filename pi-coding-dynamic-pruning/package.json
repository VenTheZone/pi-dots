{
  "name": "pi-coding-dynamic-pruning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dynamic Context Pruning extension for pi-coding-agent",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/tests/**/*.test.js",
    "check": "npm run typecheck && npm run test"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.62.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.7.3"
  }
}
