# pi-dots

Work is currently under:

- `./pi-coding-dynamic-pruning/`
- `./pi-dotfiles/`
- `./pi-mcp-access/`
- `./pi-agents/`

Per request, the actual `.pi` dotfiles for DCP integration are being deferred until after review.
